# Phase template. Copy this folder to phases/<phase_id>/ before a run.
# status: pending | running | aborted | complete
# idle stays disabled-for-phase until the phase parent ends.
# queue[].status: pending | running | plus any verdict from PROTOCOL.md

schema_version: 1
phase_id: _template
status: pending
abort_reason: ""
consecutive_infra_fails: 0
idle: disabled-for-phase
catalog: ../../catalog.yaml
protocol: ../../PROTOCOL.md
queue: []
# queue:
#   - id: example-fabric-small
#     status: pending
#     result: results/example-fabric-small.yaml
