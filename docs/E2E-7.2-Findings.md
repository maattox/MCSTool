# E2E 7.2 findings — split work for agents

**Status:** Parent Step **7.2 DONE** (2026-08-18). F1–F9 remain historical.  
**Parent:** [`MVP-Implementation-Plan.md`](MVP-Implementation-Plan.md) → [Step 7.2](MVP-Implementation-Plan.md#step-72--full-greenfield-e2e-proof).  
**Created:** 2026-08-17 from the operator’s first full Setup E2E on the **test tenancy**.  
**This file’s creation session must not implement code.** Later agents implement **only the single section marked NEXT**.

Parent Step **7.2 is DONE**. Do not re-open this file to implement new findings unless the operator files them. Do **not** start Phase 8 unless asked.

**Cost rule:** keep OCI spend at **$0** (Always Free–eligible). Do not open `0.0.0.0/0`. Do not add paid shapes, volumes, or load balancers.

---

## How agents must use this file

1. Read **this file first** (especially [Progress dashboard](#progress-dashboard), [Live test-stack access](#live-test-stack-access-this-findings-work-only), and [Agent stop protocol](#agent-stop-protocol)). Skim the parent MVP dashboard so Phase 8 is not started.  
2. Implement **only the single next incomplete section** marked **NEXT** (or a **PARALLEL-OK** section the operator explicitly assigned).  
3. Stay inside this section’s [Context budget](#context-budget-256k). Do not take on neighboring sections “while you are here.”  
4. After finishing the section:
   - Update **this file**: mark the section **DONE**, set the following incomplete section to **NEXT** (unless the operator is running PARALLEL-OK work), add a changelog line.
   - File on-box / Setup / door bugs in lab [`docs/Issues.md`](../../OCI-mc-server-manager/docs/Issues.md) **and** fix the **product path** (not only the test VM).
   - Update [`MVP-Implementation-Plan.md`](MVP-Implementation-Plan.md) Step 7.2 changelog with one line pointing here. **Do not** mark 7.2 DONE.
   - **Stop.** Do not start the next section unless the operator says so.
5. In the chat reply: what you did, how to test, what’s next, ask whether to continue.  
6. **Never create git commits.** Suggest the commit message on the section.  
7. **Do not** `tofu apply` / `tofu plan` / `tofu destroy` / OCIR push. Setup Deploy against the live test stack is **operator-only**.  
8. Do **not** put Manager UI in the lab repo. Do **not** scaffold Avalonia.

### Agent stop protocol

Between **section headings (F1–F9)** always stop for operator feedback.  
Sub-bullets inside one section may be completed together if they are required to make that section testable.

If two PARALLEL-OK agents would edit the same files, do not run them together. Collision notes are on each section.

### Operator prompt (copy-paste for a new agent)

```text
Read docs/E2E-7.2-Findings.md in OCI-mc-server. Implement only the section marked NEXT (or the PARALLEL-OK section I named).
Do not mark Step 7.2 DONE. Do not start the following findings section unless I say so.
You MAY use OCI CLI/API with profile TESTING (not DEFAULT) and SSH to the test VMs with the key named in that file. Do not tofu apply/destroy. Do not commit.
When done: update E2E-7.2-Findings.md statuses, file Issues.md if this was an on-box/Setup/door bug, stop, tell me what you did, how to test, what’s next, and ask if I want to continue.
Prompt in Agent mode (not Plan mode) unless the section is marked PLAN-FIRST.
```

---

## Live test-stack access (this findings work only)

This **overrides** the parent MVP line “agents must not SSH-bootstrap the live lab VMs” **for findings sections that say they need live access**. It does **not** authorize `tofu apply` / `tofu destroy` / OCIR push.

| Item | Value |
|------|--------|
| OCI config | `%USERPROFILE%\.oci\config` (normal location) |
| OCI profile | **`TESTING`** — test tenancy. **Do not use `DEFAULT`** (that is the operator’s other/lab tenancy). |
| OCI CLI example | `oci compute instance get --profile TESTING --instance-id <from local config>` |
| SSH user | `ubuntu` |
| SSH private key | `%USERPROFILE%\.ssh\mcmgr_ed25519_20260817_125552` (same key on **both** VMs) |
| Hosts / OCIDs / IPs | Read gitignored `data/config.local.json` (and lab private markdown only if needed). **Do not copy live OCIDs, IPs, or secrets into tracked docs or chat dumps.** |
| VM state at findings creation | **Both VMs RUNNING.** Idle agent on VM1 is **turned off**. Leave it off unless the section says to reproduce idle SoftStop; then **disable it again** before you stop. |
| `ubuntu` permissions | Recurring pitfall: `Permission denied` on `/etc/mcmgr`, `/etc/mccontrol/oci.env`, etc. Use `sudo` or fix mode/owner. Read lab [`docs/Agent-Deploy-Pitfalls.md`](../../OCI-mc-server-manager/docs/Agent-Deploy-Pitfalls.md) before SSH deploy edits. |

**Allowed**

- OCI SDK / REST / CLI with **`--profile TESTING`** (or the equivalent .NET `OciSession` profile from local config).
- SSH to VM1 and the door VM with the key above (`sudo` as needed).
- Read Compute lifecycle, VNIC / public IP assignment, Security List, Object Storage objects, systemd / `journalctl`, `ss`, firewalld, door `/api/status`.
- SoftStop / start **VM1** when a section needs to reproduce idle/handback/wake (Always Free hours; prefer stopping VM1 rather than leaving it idle-on). **Do not SoftStop the door VM** unless the section explicitly requires it — that breaks the doorbell.
- Patch **product** code (`OCI-mc-server` Setup/Manager, lab `door_vm/` / `vm_agent/` SoT) and then apply the same fix on the test VMs so the next greenfield run is clean.

**Not allowed**

- `tofu apply` / `tofu destroy` / deleting the compartment / OCIR push.
- Opening `0.0.0.0/0`, changing the operator’s **`DEFAULT`** tenancy, or using the live Forge lab.
- Committing secrets, filled `oci.env`, or live OCIDs into git.
- Leaving the idle timer enabled after a reproduce, or leaving VM1 RUNNING overnight “for later” without asking.

**429 / thrift:** [`OCI-API-Usage.md`](OCI-API-Usage.md) — exponential backoff, no 1s OCI loops, modest Object Storage chatter.

---

## Context budget (256K)

Each section is sized for **one new agent** with a **256K** context window. That is large, but it is **not** “read both repos.”

**Every agent should:**

- Read this file’s header + **only their section**.
- Read the **starting files** listed there.
- Open extra files only when a symbol / log line requires it.

**No agent should:**

- Re-read all of [`MVP-Implementation-Plan.md`](MVP-Implementation-Plan.md), [`Blazor-UI-Migration-Plan.md`](Blazor-UI-Migration-Plan.md), or lab `PRODUCT-IDEAS.md` unless the section names a specific heading.
- Dump entire `door_vm/` or `onbox/mcmgr/` into context “for background.”
- Implement a different section’s bullets.

If you are running out of context, **stop and hand off** rather than skimming the rest of the findings list.

---

## What already happened (do not re-fix)

Operator ran Setup on the test tenancy (2026-08-17):

1. First Deploy failed waiting for `/etc/mcmgr/cloud-init-done` — **SETUP-ISSUE-5**, already **fixed** (`sudo -n test -f`). Do not reopen.
2. Operator resumed from that point; **Deploy then finished completely.**
3. After **Close**, Manager opened but local config failed until a full restart.
4. Play-path / idle / Start behavior was incomplete (sections F7–F9).

Idle SoftStop **did** fire after 15 minutes of no Minecraft activity during that dogfood. The play-IP / door / subsequent Start path did **not** behave.

---

## Progress dashboard

| ID | Section | Status | Parallel? | Live SSH/OCI? |
|----|---------|--------|-----------|----------------|
| **F1** | Setup deploy screen UX (elapsed, “takes time”, spacing, slim scrollbar) | **DONE** | SEQUENTIAL vs F2/F3 (same Razor/CSS) | No |
| **F2** | Calibrate deploy % / remaining time from a timed log | **DONE** | After **F1** | No |
| **F3** | Drop Minecraft username; add VM1 2/12 vs 4/24 picker | **DONE** | After **F1** (same wizard). May follow or precede F2 if F2 stays off Razor fields | No |
| **F4** | After Setup Close, Manager must load `config.local.json` without restart | **DONE** | **PARALLEL-OK** vs F1–F3, F5, F6 | No |
| **F5** | Start/Stop/Restart enablement + “Starting…” / “Stopping…” | **DONE** | **PARALLEL-OK** vs F1–F4, F6 | Optional read-only status |
| **F6** | Docs: usage ledger does not survive destroy + redeploy | **DONE** | **PARALLEL-OK** | No |
| **F7** | Door / reserved IP: first-connect timeouts while VM1 already up | **DONE** | **PARALLEL-OK** vs UI/docs; **before F8** | **Yes** |
| **F8** | After idle SoftStop: no doorbell, no wake (DOOR_IDLE, both IPs time out) | **DONE** | After **F7** | **Yes** (may SoftStop VM1) |
| **F9** | Manager Start after idle: stuck “starting”, Minecraft flaky | **DONE** | After **F8** | **Yes** |

**Current NEXT:** none — F1–F9 are **DONE**. Parent Step **7.2** is **DONE** (2026-08-18 operator play-path + DOOR-ISSUE-9 / SETUP-ISSUE-7).

---

## F1 — Setup deploy screen UX

**Status:** DONE  
**Depends on:** nothing  
**Parallel:** SEQUENTIAL vs **F2** and **F3** (all touch `SetupWizard.razor` / wizard CSS).  
**Live SSH/OCI:** no.

### Operator notes

- Show **Time elapsed** once Deploy has begun.
- Before Deploy **and** while it runs, a clear note that deployment can take a long time.
- **Deploy log** title overlaps the bottom of the **Plan summary** box. Add space between the plan summary and the deploy log.
- Setup wizard still uses the default vertical scrollbar. It should use the **same slimmer scrollbar** as the Manager tab body.

### Starting files (read these, not the whole UI tree)

- `src/McManager.Hybrid/Components/Setup/SetupWizard.razor` (summary/deploy step)
- `src/McManager.Hybrid/ViewModels/SetupWizardViewModel.cs` (progress / busy / lock)
- `src/McManager.Hybrid/wwwroot/css/app.css` — `.mcm-wizard-*` and the Manager `.mcm-app-manage .mcm-tab-body` scrollbar rules (~tab-scrollbar-size)
- Optional: destroy-dialog progress row as a layout reference only

### Do

1. **Time elapsed:** start a clock when Deploy actually begins; show `Time elapsed: H:MM:SS` (or `M:SS`) on the deploy progress row for the whole apply/bootstrap. Pause/stop when the pipeline finishes or fails. Do not count time sitting on the summary step before Deploy.
2. **Duration copy:** novice-readable, visible **before** Deploy (summary step) **and** during the run. Example intent: creating the cloud computers and installing Minecraft often takes a long time; leave the window open. Do not promise a fake ETA here (that is **F2**).
3. **Spacing:** plan summary and deploy log must not overlap at the default wizard size. Increase gap / stacking so the “Deploy log” label sits clearly below the plan box.
4. **Scrollbar:** apply the Manager thin overlay-style thumb (gutter + `::-webkit-scrollbar` + `scrollbar-width: thin`) to wizard body / log panes so Setup does not show the thick OS default bar. Match Manager; do not invent a third style.

Do **not** retune percent weights or add remaining-time estimates (F2). Do **not** add the VM1 shape picker or remove the username field (F3).

### Test

- Open Setup to the last step at the default window size: no overlap between plan summary and “Deploy log”.
- Wizard / log overflow uses the slim thumb.
- Dry-run Deploy (`MCMANAGER_TOFU_DRY_RUN=1`) shows elapsed time ticking and the “takes time” copy before and during the run.

### Done when

Elapsed + duration copy + spacing + slim scrollbar are in the wizard; `dotnet build` clean.

**Suggested commit:** `Show Setup deploy elapsed time and fix wizard log overlap and scrollbar.`

**Changelog:** 2026-08-17 — Elapsed clock starts on Deploy (pauses when the pipeline finishes/fails or while waiting on capacity; resumes on retry). Duration hint on the summary step. Plan summary and deploy log stacked with a 20px gap and clipped panes so the Deploy log label no longer overlaps the plan box. Wizard body + log panes use the same thin overlay scrollbar as Manage.

---

## F2 — Calibrate deploy progress / remaining time

**Status:** DONE  
**Depends on:** **F1** (elapsed UI and deploy-row layout).  
**Parallel:** after F1. Avoid editing the username/shape fields (F3).  
**Live SSH/OCI:** no.

### Operator notes

Use a **timestamped test Deploy log** to make the percent bar and/or remaining-time copy match real stage durations. Equal stage weights are misleading (`SetupApplyStage.Percent` today is `index / 7`).

### Timed log (not in this file yet)

The operator had a test Deploy log for this work. It was **not pasted** into the session that created this findings file.

**Before changing weights, the F2 agent must obtain a timestamped log.** In order:

1. look for `doc/deploy-log.txt`
2. Ask the operator to **Copy deploy log** from the finished wizard (lines are already `[HH:mm:ss] …`) and paste it into chat, **or**
3. If they add it under [Appendix A](#appendix-a--timed-deploy-log), use that, **or**
4. As a last resort, reconstruct durations from `%LOCALAPPDATA%\McManager\tofu\<stack-id>\` logs **without** publishing secrets/OCIDs.

Do **not** invent minutes. If no log is available, **stop and ask** — do not ship a guessed ETA.

IMPORTANT: note that deployment was done in two separate sessions and the deploy log is the result of stitching the logs from both sessions together. The first session failed at "waiting for /etc/mcmgr/cloud-init-done" due to permission issues that have already been fixed. `cloud-init` did not actually take 19 minutes. The time for that step should be estimated as 3 minutes. The second deployment session then begins about 40 minutes later. This does not mean deployment took 40 minutes. Make sure to account for that when analyzing the deploy log.

Strip live OCIDs, IPs, and Auth Token fragments before quoting log lines in tracked docs.

### Starting files

- `src/McManager.Core/Setup/SetupApplyStage.cs`
- `src/McManager.Core/Setup/SetupDeployOrchestrator.cs` (when percent/captions are reported)
- `src/McManager.Hybrid/ViewModels/SetupWizardViewModel.cs`
- F1’s elapsed row on the deploy screen

### Do

1. Parse the timed log into stage durations (tofu apply, cloud-init wait, door bootstrap, VM1/Minecraft install, Object Storage meta, Function/OCIR, config write). Resume-from-`tofu_applied` runs are **not** a full-deploy sample — prefer a log that includes apply, or label estimates as “after VMs exist.”
2. Weight the progress bar by **observed time**, not equal stages. Tofu apply + VM1 install will dominate.
3. If remaining time can be shown cleanly: `About N minutes left` (range OK) from elapsed + current stage + weights. Prefer honest ranges over a precise countdown. PRODUCT-IDEAS: ETA was deferred until a timed run existed — this **is** that run.
4. Keep timestamps on log lines.

### Test

- Dry-run: bar still reaches 100% and does not jump backwards.
- Using the timed log as a table, a full-deploy simulation should not sit at 50% for most of tofu apply. Document the weight table in the section changelog.

### Done when

Percent (and remaining-time if shipped) is driven by the timed log; changelog lists per-stage minutes used.

**Suggested commit:** `Weight Setup deploy progress from a timed E2E log.`

**Changelog:** 2026-08-17 — Progress weights from `docs/deploy-log.txt` (stitched two sessions; ignored the ~40 min gap; cloud-init **3 min** not the 19 min SETUP-ISSUE-5 false wait). Bar interpolates inside the current stage and never jumps backwards. Remaining copy is a range (`About 10–15 minutes left` at a full-deploy start). Hidden on dry-run. Function/OCIR had no timed sample (skipped) — 15s floor. See Appendix A.

| Stage | Seconds used | % when complete |
|-------|-------------:|----------------:|
| tofu apply | 60 (~48s apply in log; init/plan start truncated) | 8% |
| cloud-init wait | 180 (operator estimate) | 33% |
| door bootstrap | 300 (4m54s) | 73% |
| VM1 install + guest repair | 150 (~2m + 25s) | 94% |
| Object Storage seed | 15 (1s observed; floor) | 96% |
| Function/OCIR | 15 (skipped in E2E; floor) | 98% |
| config write | 15 (floor) | 100% |

---

## F3 — Setup inputs: drop Minecraft username; add VM1 shape

**Status:** DONE  
**Depends on:** **F1** preferred first (same summary step in `SetupWizard.razor`). Can run after F1 even if F2 is still open, if F2 is not editing that form.  
**Parallel:** not with F1.  
**Live SSH/OCI:** no. Do not apply a new stack to test the picker.

### Operator notes

- In-game whitelist is off; **stop asking** for a Minecraft username in Setup (including optional).
- Setup must offer VM1 size: **2 OCPU / 12 GB** or **4 OCPU / 24 GB**.

### Product intent (read only this heading)

Lab `PRODUCT-IDEAS.md` → **VM1 shape (Setup)** (not v1 day-2 resize). Default remains **4/24**; 2/12 is the smaller Always Free–eligible choice. Wire `vm1_ocpus` / `vm1_memory_gb` into OpenTofu. **Revert** the TEMPORARY `infra/variables.tf` defaults from **2/12 → 4/24** once the picker writes those variables (today HCL defaults 2/12 because Setup omits them).

### Starting files

- `src/McManager.Hybrid/Components/Setup/SetupWizard.razor` (summary fields)
- `src/McManager.Hybrid/ViewModels/SetupWizardViewModel.cs`
- `src/McManager.Core/Config/SetupWizardState.cs`
- `src/McManager.Core/Setup/MinecraftUsername.cs` and call sites (`SetupDeployOrchestrator`, `SetupBootstrapService`)
- `src/McManager.Core/Setup/TfvarsWriter.cs` (does **not** write `vm1_ocpus` / `vm1_memory_gb` today)
- `src/McManager.Core/Setup/InfraPlanSummary.cs` (still says TEMPORARY 2/12)
- `infra/variables.tf`, `infra/README.md`
- Guide Setup / plan-summary copy if it mentions username or the 2/12 test default

### Do

1. **Remove** the Minecraft username field from the wizard, resume JSON, validation, and bootstrap seeding of `whitelist.json` from that name. Do not leave a hidden required field. In-game `white-list=false` stays (Step 4.3).
2. **Add** a required VM1 shape choice **before** apply (summary or a nearby step): only `2 / 12` and `4 / 24`. Default **4 / 24**. Confirm copy + plan summary must match the choice (hours/headroom in novice language).
3. Write `vm1_ocpus` and `vm1_memory_gb` from the wizard into LocalAppData tfvars via `TfvarsWriter`.
4. Restore HCL **defaults** to **4 and 24**; the picker overrides. Distinct from v1 Danger Zone resize — do not build day-2 scaling.

### Test

- Wizard has no username field; validation does not mention it.
- Plan summary shows the selected shape.
- Generated tfvars contain `vm1_ocpus` / `vm1_memory_gb`. `tofu validate` in `infra/` still passes.
- Dry-run Deploy still works. **No live apply.**

### Done when

Username is gone; shape is collected and passed to OpenTofu; HCL defaults are 4/24 again; docs/plan summary no longer advertise the 3.3 2/12 temporary default as the product shape.

**Suggested commit:** `Add Setup VM1 2/12 vs 4/24 picker and stop collecting a Minecraft username.`

**Changelog:** 2026-08-17 — Removed Minecraft username from the wizard, resume JSON, Deploy validation, and `whitelist.json` seed. In-game `white-list=false` unchanged. Summary step has a required VM1 size radio: **4 OCPU / 24 GB** (default) or **2 OCPU / 12 GB**, with hours/headroom copy. Confirm checkbox and plan summary follow the choice. `TfvarsWriter` emits `vm1_ocpus` / `vm1_memory_gb`. Capacity probe uses the selected size. `infra/variables.tf` defaults restored to **4 / 24**.

---

## F4 — Load local config after Setup Close (no restart)

**Status:** DONE  
**Depends on:** nothing  
**Parallel:** **PARALLEL-OK** (host/DI files; do not edit SetupWizard layout except Close/reload wiring).  
**Live SSH/OCI:** no.

### Operator notes

After Deploy finished, **Close** opened Manager, but it reported that **local config failed to load**. Closing the app and reopening loaded config correctly.

### Likely cause (verify; do not assume)

`LocalConfigHost.Reload()` runs on wizard Close, but **`ManageCloudServices` and `MainViewModel` are singletons** built at first resolve — often while `config.local.json` did **not** exist (first-run → Setup). Reload updates the host; it does **not** rebuild OCI/door clients or the MainViewModel’s captured `_config`. Connect-existing has the same Reload-then-EnterManage pattern and may share the bug.

### Starting files

- `src/McManager.Hybrid/Components/Setup/SetupWizard.razor` (`Close`)
- `src/McManager.Hybrid/HybridShell.cs`
- `src/McManager.Hybrid/LocalConfigHost.cs`
- `src/McManager.Hybrid/ManageCloudServices.cs`
- `src/McManager.Hybrid/App.xaml.cs` (`RegisterManageServices`)
- `src/McManager.Hybrid/ViewModels/MainViewModel.cs` (ctor captures config/clients)
- `src/McManager.Hybrid/ViewModels/FirstRunViewModel.cs` (detect → Reload → manage)
- `src/McManager.Hybrid/ViewModels/DestroyInfrastructureViewModel.cs` (Reload after delete — do not break teardown)

### Do

After a successful Setup (and Connect-existing), entering Manage must see the new `config.local.json` **without** restarting the process: reload file, rebuild cloud clients, and give MainViewModel (and tabs that captured clients) a working session.

Prefer a small, explicit rebuild/rebind over “tell the user to restart.” Destroy-infrastructure Reload must still leave Manage/first-run consistent when config is **removed**.

### Test

- From first-run, dry-run does **not** write config — do not use dry-run as the only proof.
- Code-level: after Close, `LocalConfigHost.HasManageConfig` is true **and** `ManageCloudServices.Session` / `Door` / `MainViewModel.ConfigLoaded` are true when a real `config.local.json` exists.
- Operator: one Close after a real deploy (or a local fixture write + Close) opens Manage with play IP and no “failed to load” toast.

### Done when

First Manage entry after Setup Close works without killing the process.

**Suggested commit:** `Reload manage clients after Setup writes config.local.json.`

**Changelog:** 2026-08-17 — Root cause: `ManageCloudServices` / manage ViewModels are singletons; `LocalConfigHost.Reload()` did not rebuild OCI/door clients or captured `_config`. `App.razor` also mounted `DestroyInfrastructureDialog` on first-run, which constructed `MainViewModel` before `config.local.json` existed. Added `ManageSession.ReloadFromDisk()` (reload file → rebuild clients → rebind ViewModels). Setup Close, first-run Connect-existing / “I already have a stack”, Advanced Auto-detect, and destroy-success now call it. Destroy dialog is Manage-only so first-run/Setup do not construct MainViewModel early. `dotnet build` clean. Not an on-box/Setup/door bug — no `Issues.md` row.

---

## F5 — Power buttons and in-progress status copy

**Status:** DONE  
**Depends on:** nothing (UI). Door-state-after-deploy may also be **F7**; still fix chrome here.  
**Parallel:** **PARALLEL-OK**.  
**Live SSH/OCI:** optional read-only poll to confirm states; do not SoftStop for this section.

### Operator notes

1. First Manage open after deploy: **both VMs were already on**, but **Start, Stop, and Restart were all active**. Only **Stop** and **Restart** should have been active. After a Minecraft client connect to the reserved IP, **Start** finally disabled.
2. While Start/Stop is in progress, the **button and/or status panel** should say **Starting…** / **Stopping…**. Today there is no in-progress text.

### Likely cause (verify)

`MainViewModel.UpdateCommandFlags`: **Start** is enabled when the door is not `PLAYABLE` / not `STARTING`. After Setup the door is often still **`DOOR_IDLE`** even when VM1 is **RUNNING** and Minecraft is up → Start stays on. Connecting via the play IP may move the door to `PLAYABLE` and then Start disables.

Novice **Status** is Running/Stopped (joinable), not raw door enum. Start must not look available when the server is already on.

`_powerActionInFlight` already greys buttons but does not change Status/button captions.

### Starting files

- `src/McManager.Hybrid/ViewModels/MainViewModel.cs` (`CanStart` / `CanStop` / `CanRestart`, `ApplyNoviceStatus`, Start/Stop methods)
- `src/McManager.Hybrid/Components/Layout/MainLayout.razor` (Status + power row)
- `src/McManager.Core/Services/DoorStatus.cs`

### Do

1. **Enablement:** If VM1 is RUNNING (and/or Minecraft is joinable), **Start is off**; Stop + Restart on. Do not require a client connect to refresh that. Treat `DOOR_IDLE` + VM1 RUNNING as “already on” for Start, not as “press Start to wake.” Align with operator rule: first status load may grey all three; after that, in-flight greys all three; otherwise Start vs Stop vs Restart follow playability.
2. **Copy:** While a start is in flight, Status and/or the Start control shows **Starting…**. While a stop is in flight, **Stopping…**. Restart may say **Restarting…** if cheap. Clear when the action finishes or fails (toast already exists).

Do **not** implement the post-v1 Start checklist. Do **not** redesign Advanced door labels except as needed for the in-progress string.

### Test

- With VM1 RUNNING and door `DOOR_IDLE` or `PLAYABLE`, Start is disabled; Stop and Restart enabled.
- With VM1 STOPPED and door idle, Start enabled; Stop/Restart off (unless a documented starting state).
- Click Start/Stop: in-progress text appears immediately, not only after the next door poll.

### Done when

Button enablement matches VM-on vs VM-off without needing a Minecraft connect; Starting… / Stopping… is visible during the action.

**Suggested commit:** `Disable Start when the server is already on and show Starting/Stopping.`

**Changelog:** 2026-08-17 — Root cause: `CanStart` treated any non-PLAYABLE / non-STARTING door (including `DOOR_IDLE`) as “press Start,” so after Setup with VM1 already RUNNING, Start stayed enabled until a client connect moved the door to PLAYABLE. `_powerActionInFlight` greyed buttons but left Status/captions as Running/Stopped. Start is now off when VM1 is RUNNING/STARTING/PROVISIONING or the door is PLAYABLE/STARTING; Stop follows the same “already on or coming up” set; Restart still requires VM1 RUNNING. First status load still greys all three; in-flight greys all three. Status + the matching button show **Starting…** / **Stopping…** / **Restarting…** immediately (busy cobalt, not green/red); door polls during the wait do not overwrite that copy. Steady novice Status remains Running only when joinable (`PLAYABLE`). Not an on-box/Setup/door bug — no `Issues.md` row. `dotnet build` clean.

---

## F6 — Document: usage ledger does not carry across destroy + redeploy

**Status:** DONE  
**Depends on:** nothing  
**Parallel:** **PARALLEL-OK** (docs only).  
**Live SSH/OCI:** no. **Do not implement** ledger migration.

### Operator notes

If a deployment is **deleted** so a new infra stack can be created from scratch, the **usage ledger from the first deployment does not carry over**. The new stack’s ledger is **not accurate** for the month until a **new month** begins, because some Always Free hours were already consumed by the deleted stack.

This is **documentation only** for MVP. Do not build ledger import/export now.

### Starting files (docs)

- [`Guide.md`](Guide.md) → Tear down and redeploy
- [`Automated-Infrastructure-Deployment.md`](Automated-Infrastructure-Deployment.md) §12.4 / destroy notes
- [`Local-Config.md`](Local-Config.md) (destroy deletes local config; bucket goes with tofu destroy)
- [`Contracts-Object-Storage.md`](Contracts-Object-Storage.md) — one caution sentence is enough
- Parent [`MVP-Implementation-Plan.md`](MVP-Implementation-Plan.md) Step 7.2 notes if useful

### Do

Add a short operator-facing warning: Delete infrastructure wipes the product bucket (including `ledger/usage.json`). A new Setup starts a **new** ledger at zero. Oracle Always Free **OCPU-hours for the calendar month are still consumed** by the old VMs’ uptime. Manager usage / leftover-bank on the new stack will look “too high” until the next month (or until the operator understands the two clocks). World backups in that bucket are also gone.

Keep it novice-readable in the Guide; technical one-liner in contracts/destroy docs. No live OCIDs.

### Test

- Guide tear-down section mentions ledger/hours vs Manager display.
- No code change required.

### Done when

The warning is in the happy-path tear-down docs (and a contract/destroy cross-link).

**Suggested commit:** `Warn that destroy+redeploy resets the usage ledger mid-month.`

**Changelog:** 2026-08-17 — Guide tear-down warns that Delete wipes the bucket (usage history + world backups); a new Setup starts usage at zero while Oracle Always Free OCPU-hours for the calendar month already include the old VMs. Same caution in Automated-Infrastructure-Deployment §12.4, Local-Config destroy notes, Contracts-Object-Storage, and Step 7.2 teardown notes. Docs only — no ledger import. Not an on-box/Setup/door bug — no `Issues.md` row.

---

## F7 — Door / reserved IP timeouts while VM1 is already up

**Status:** DONE  
**Depends on:** nothing (first live-stack section). Do **F7 before F8**.  
**Parallel:** **PARALLEL-OK** vs F1–F6. Not with F8/F9.  
**Live SSH/OCI:** **yes** (`TESTING` + SSH key above). Both VMs are RUNNING; idle is off — **leave idle off**.

### Operator notes

VM1 and Server was **already on** after the initial automated infrastructure deployment. VM1 had not been stopped or started since its initial creation and setup. Minecraft client:

1. Reserved **play IP** → failed, `Connection timed out: getsockopt`.
2. VM1 **ephemeral** public IP → **worked**.
3. Door VM **ephemeral** public IP → **timed out**.
4. Reserved play IP **again** → then **worked**.

So: game was reachable on VM1’s own public IP; the **door** was not answering 25565; the **reserved IP** failed until something changed after the VM1-direct connect.

### What to prove (write the answer in the section changelog)

Do not guess. On the test stack, record:

- Which VNIC holds the **reserved public IP** (VM1 secondary vs door secondary) — OCI CLI/API `TESTING`.
- Whether each guest has the secondary play address in netplan / `ip addr`.
- Whether `mccontrol` is listening on door `:25565` (TCP **and** UDP) when `DOOR_IDLE`.
- Security List + firewalld for 25565 to **both** public destinations the client tried.
- Door `/api/status` and `journalctl -u mccontrol` around the failed client connects.
- Whether Setup left the door in `DOOR_IDLE` **without** parking the reserved IP on the door when VM1 was already playable (or the reverse: IP on VM1 but door still idle).

**Hypothesis to test (not a conclusion):** reserved IP was still on the door (or nowhere), door process/firewall did not accept Minecraft, VM1 ephemeral worked because the game listens on VM1; a later IP move or door wake made the reserved IP work.

### Starting files (lab SoT + product bootstrap)

- Lab `docs/Door-VM-Control-Plane.md` (behavior)
- Lab `door_vm/` — `mccontrol` listen / `DOOR_IDLE` / `ip_to_vm1.sh` / `ip_to_vm2.sh`
- Product Setup door install: `src/McManager.Core/Setup/SetupBootstrapService.cs` (forces `door_state=DOOR_IDLE` during door bootstrap)
- `src/McManager.Core/Services/TroubleshootingService.cs` (park IP)
- Lab `docs/Issues.md` (SETUP-ISSUE-1, DOOR-ISSUE-4/5 — do not re-open fixed CHDIR)
- Lab `docs/Operator-Troubleshooting.md` for copy-paste patterns

### Do

1. Diagnose with OCI + SSH; keep secrets out of tracked files.
2. **Fix the product path** so the next greenfield deploy does not need a lucky second connect: reserved play IP + door MOTD/listen when idle, and reserved IP + VM1 when playable. If Setup finishes with VM1 playable, either move the IP to VM1 **or** leave a working doorbell only when VM1 is down — but **do not** leave a black hole (timeout on both door and reserved IP).
3. File a new `DOOR-ISSUE-*` / `SETUP-ISSUE-*` in lab `Issues.md`.
4. If the test VM needs a repair script, the same logic must land in `door_vm/` and/or Setup bootstrap.

Do not start a full idle SoftStop reproduce here (that is F8). Do not SoftStop the door.

### Test

- From the operator PC (allowlisted): Minecraft to **reserved play IP** succeeds while VM1 is RUNNING and the game is up, **without** first connecting to VM1 ephemeral.
- Door ephemeral: document expected behavior (doorbell only when IP/listen is on the door; timeout may be correct if the reserved IP is on VM1 and the door does not bind 25565 on the ephemeral). If timeout is **incorrect**, fix it.
- Advanced door status matches reality.

### Done when

Root cause is written; product fix is in git; test stack matches; Issues.md updated.

**Suggested commit:** `Fix play-IP / door listen so reserved IP works without a VM1-direct connect first.`

**Changelog:** 2026-08-17 — Live diagnosis (`TESTING`): after Setup, reserved IP was still on the **door** secondary; both guests had play netplan; SL 25565 TCP+UDP for admin `/32`; VM1 firewalld 25565 tcp/udp; door iptables TCP 25565 then REJECT (no UDP). `mccontrol` listened TCP `:25565` (`DOOR_IDLE`). First client login woke the door; `start_vm1.sh` `START` on already-RUNNING VM1 → **409**; wake never called `ip_to_vm1`. mcdoor single-thread `recv` with no timeout filled listen backlog 16 (`Recv-Q` = 17) so reserved IP **and** door ephemeral TCP-timed out; VM1 ephemeral (Java) still worked. Later idle/Start path moved the IP to VM1 (`PLAYABLE`) — that is why a retry then joined. **Fix:** Setup `promote_playable.sh` after VM1 repair (IP to VM1 **then** `PLAYABLE`); do not force `DOOR_IDLE` at deploy end; `start_vm1.sh` no-op if RUNNING/STARTING; mcdoor 8s I/O timeout + backlog 128. Filed **SETUP-ISSUE-6** / **DOOR-ISSUE-6**. Test stack: IP on VM1, door `PLAYABLE`, idle **off**, mccontrol rebuilt. Door ephemeral `:25565` is MOTD (not the Vanilla world) and must not TCP-timeout.

---

## F8 — After idle SoftStop: doorbell and wake do not work

**Status:** DONE  
**Depends on:** **F7** (same VMs and IP/listen facts).  
**Parallel:** SEQUENTIAL after F7.  
**Live SSH/OCI:** **yes**. Idle is currently **off** — enable only to reproduce, then **disable again**.

### Operator notes

Idle agent **did** SoftStop VM1 after 15 minutes empty. After that:

- Reserved play IP → Minecraft **timeout**, **did not** start VM1.
- Door **ephemeral** public IP → **timeout**, **did not** start VM1.
- Manager Advanced: door service state **`DOOR_IDLE`**.

So the doorbell did not answer, and wake did not run. This may be door listen / reserved IP **not moved to the door** after VM1 stop, rather than “wrong IP” alone.

### Starting files

- Lab `door_vm/scripts/reconcile_vm1.sh` (handback when VM1 STOPPED)
- Lab `door_vm/oci/ip_to_vm2.sh`
- Lab `vm_agent/` SoftStop path (does **not** move the public IP; door reconcile should)
- Lab `docs/Door-VM-Control-Plane.md`
- F7 changelog (do not re-diagnose from zero if F7 already recorded IP owner / listen)

### Do

1. Reproduce **once**: empty server or Minecraft down → idle timeout **or** an equivalent graceful VM1 SoftStop that should trigger the same handback. Prefer a **short** idle timeout for the test, then restore the operator’s timeout and **disable idle** again.
2. When VM1 is STOPPED, prove: reserved IP on **door** secondary, door listening 25565 TCP/UDP, client ping/handshake hits `mccontrol`, `/api/wake` (or connect) starts VM1.
3. Fix product `door_vm/` / reconcile / Setup enablement of `mccontrol-reconcile.timer` if handback never ran. Fix listen/bind if IP was correct but nothing accepted 25565.
4. File `DOOR-ISSUE-*` (or update F7’s issue if it is the same root cause).
5. Leave **idle disabled** and VM1 in a state the operator asked for (both were RUNNING at findings creation; after this test, ask before leaving VM1 up).

### Test

- VM1 STOPPED → reserved play IP shows starting MOTD / does not TCP-timeout.
- A connect or Manager Start wakes VM1 (F9 covers “stuck starting”).
- Door ephemeral behavior documented vs reserved IP.

### Done when

Idle (or equivalent SoftStop) parks a working doorbell on the reserved IP; Issues.md + product path updated; idle left off.

**Suggested commit:** `Hand reserved play IP back to a listening door after VM1 idle SoftStop.`

**Changelog:** 2026-08-17 — Equivalent SoftStop (Minecraft stopped, then OCI SOFTSTOP; idle left off). Reconcile **did** POST idle-empty on PLAYABLE+STOPPING and parked the reserved IP on the **door**; MOTD TCP ~0.03s on reserved IP **and** door ephemeral (`Server offline…`). Original E2E timeouts were **DOOR-ISSUE-6** stall while Setup still had the IP on the door. After F7 (IP on VM1), the remaining hole was `stop_vm1.sh` **409** while VM1 was already STOPPING, and reconcile **skipping** `ip_to_vm2` when already `DOOR_IDLE`. **Fix:** `stop_vm1.sh` skip STOPPED/STOPPING; reconcile still `ip_to_vm2` when idle/exhausted + VM1 down; `install.sh` ships scripts + reconcile timer; Setup repair deploys those scripts. Filed **DOOR-ISSUE-7**. `POST /api/wake` started VM1 (RUNNING); wait_forge/joinable is **F9**. Boot **re-armed** idle — disabled again. Left **VM1 STOPPED**, IP on door, `DOOR_IDLE`, idle **off**. Java doorbell is TCP-only.

---

## F9 — Manager Start after idle: stuck starting, Minecraft flaky

**Status:** DONE  
**Depends on:** **F8** (wake/handback).  
**Parallel:** SEQUENTIAL after F8.  
**Live SSH/OCI:** **yes**. Idle off unless you need another stop/start cycle; disable when done.

### Operator notes

After idle stopped VM1 for the first time since the intial VM create and setup during automated deployment, Manager **Start**:

- VM1 **started fine**.
- Status stayed **starting** **> 10 minutes**.
- Minecraft never became reachable from the client.

After idle stopped VM1 **again**, a **third** Manager Start **worked** (VM1 + Minecraft). Same operator actions, inconsistent results.

This issue may be related to previous issues in F7 and F8.

Note that the initial problem occured under the unique circumstances of the initial automated deployment. VM1 had been created and set up during automated deployment and had not yet been stopped a single time.

### Starting files

- `src/McManager.Hybrid/ViewModels/MainViewModel.cs` (`StartAsync`, wait-for-playable)
- Lab `door_vm/oci/wait_forge.sh` + `diagnose_wait_forge.sh`
- Product `onbox/mcmgr/` unit / `minecraft.service` (CHDIR is **SETUP-ISSUE-4**, already fixed — confirm it did not regress)
- Lab `docs/Issues.md` DOOR-ISSUE-5 (fixed as CHDIR; this may be a **new** wait_forge / boot race)
- Lab `docs/Operator-Troubleshooting.md`

### Do

1. Reproduce Start-after-stop with logs: door state machine (`STARTING` / `DEGRADED` / `PLAYABLE`), `wait_forge`, `journalctl -u minecraft`, `ss -lntp | grep 25565`, cloud-init not involved.
2. Separate **VM1 RUNNING but game not listening** from **game up but door never leaves STARTING** from **Manager UI stuck while door is already PLAYABLE**.
3. Fix the product path (door wait timeout, Minecraft enable-on-boot, Setup `systemctl enable minecraft`, guest netplan, Manager wait condition). Flaky “third try worked” often means a race (IP move vs listen, first-join MOTD, wait_forge timeout too short, unit not enabled).
4. File a new issue if this is not F8. Sticky DEGRADED still has Troubleshooting one-shots — product should not require them on every Start.
5. In-progress **Starting…** copy is **F5**; do not skip F5’s chrome work here, but you may hook the same status string if F5 already landed.

### Test

- From VM1 STOPPED + working doorbell (F8): Manager Start → VM1 RUNNING → Minecraft joinable on reserved IP in a **bounded** time (document typical minutes; 10+ min with no game is failure).
- Repeat **twice** so it is not a one-off.

### Done when

Start-after-idle is reliable enough for E2E sign-off; root cause and fix are in Issues.md + product code.

**Suggested commit:** `Make door wait / Minecraft start reliable after idle SoftStop.`

**Changelog:** 2026-08-17 — Live: after F8, first Start-after-stop already reached PLAYABLE (~40s; Minecraft **enabled**, no CHDIR). Original E2E “Starting… >10 min / third try” was `wait_forge` burning its **600s** while VM1 was still STARTING, plus `/dev/tcp` hangs, then F5 **Start disabled** on DEGRADED+RUNNING so retry required Stop. **Fix:** `start_vm1.sh` waits for RUNNING; `wait_forge` 5s probe cap; reconcile `promote_playable` when DEGRADED/STARTING + game TCP; Manager Start enabled on DEGRADED; Start wait 30 min. Two post-fix cycles: PLAYABLE **~109s** and **~76s**, reserved IP TCP OK. Filed **DOOR-ISSUE-8**. Left **VM1 STOPPED**, IP on door, `DOOR_IDLE`, idle **off**. Typical joinable **1–2 min**; 10+ min with no game is still failure.

---

## What “Step 7.2 DONE” still requires

**Met (2026-08-18).** Operator second greenfield play path (Setup → join reserved IP → idle SoftStop + backup → wake → Restart → Stop) plus **DOOR-ISSUE-9** (async idle-empty; `POST` **202** in ~0.5 ms) and **SETUP-ISSUE-7** (mask `netfilter-persistent` so firewalld keeps 25565 after SoftStop reboot). Do not re-open this file to implement new findings unless the operator files them.

---

## Plan changelog

| Date | Note |
|------|------|
| 2026-08-18 | Parent **Step 7.2 DONE.** Operator play-path pass + **DOOR-ISSUE-9** (async Stop) + **SETUP-ISSUE-7** (firewalld vs netfilter-persistent). Do not start Phase 8 unless asked. |
| 2026-08-17 | **F7 DONE.** Setup left reserved IP on the door + `DOOR_IDLE` while VM1 was already playable; wake `START` 409 skipped `ip_to_vm1`; mcdoor accept stall caused TCP timeouts. Product: `promote_playable.sh`, `start_vm1.sh` already-up no-op, mcdoor I/O timeouts. **SETUP-ISSUE-6** / **DOOR-ISSUE-6**. **NEXT = F8.** Do not start F8/F9 until the operator says so. Do not mark MVP Step 7.2 DONE. |
| 2026-08-17 | **F6 DONE.** Guide + destroy/contract docs: destroy+redeploy resets the usage ledger; Oracle monthly Always Free hours do not. **NEXT = F7.** Do not start F8/F9 until F7. Do not mark MVP Step 7.2 DONE. |
| 2026-08-17 | **F5 DONE.** Start disabled when VM1 is already on (`DOOR_IDLE` + RUNNING no longer looks like Wake); Status/buttons show Starting… / Stopping… / Restarting… while a power action is in flight. **NEXT = F6.** PARALLEL-OK: F6, F7. Do not start F8/F9 until F7. Do not mark MVP Step 7.2 DONE. |
| 2026-08-17 | **F4 DONE.** Setup Close / Connect-existing reload `config.local.json` and rebuild manage clients without restart. **NEXT = F5.** PARALLEL-OK: F6, F7 (F5 owns power-button chrome). Do not start F8/F9 until F7. Do not mark MVP Step 7.2 DONE. |
| 2026-08-17 | **F3 DONE.** Setup VM1 2/12 vs 4/24 picker; Minecraft username removed; HCL defaults 4/24. **NEXT = F4.** PARALLEL-OK: F5, F6, F7 (F4 owns Close/reload host files). Do not start F8/F9 until F7. Do not mark MVP Step 7.2 DONE. |
| 2026-08-17 | **F2 DONE.** Time-weighted Setup deploy % + remaining-time range from the timed E2E log (cloud-init 3 min; door ~5 min dominates). **NEXT = F3.** PARALLEL-OK: F4, F5, F6, F7 (F3 owns SetupWizard fields). Do not start F8/F9 until F7. Do not mark MVP Step 7.2 DONE. |
| 2026-08-17 | **F1 DONE.** Setup deploy elapsed + duration copy + plan/log spacing + slim scrollbar. **NEXT = F2.** PARALLEL-OK: F4, F5, F6, F7 (do not edit SetupWizard layout). Do not start F8/F9 until F7. Do not mark MVP Step 7.2 DONE. |
| 2026-08-17 | Created from operator E2E notes. SETUP-ISSUE-5 already fixed. **NEXT = F1.** PARALLEL-OK: F4, F5, F6, F7. Do not start F8/F9 until F7. Do not mark MVP Step 7.2 DONE. |

---

## Appendix A — Timed deploy log

**Status:** used by F2 (2026-08-17). Source was `docs/deploy-log.txt` (stitched first failed session + resume). Do not put Auth Tokens, PEM material, or full OCIDs here.

Redacted stage table (no live IPs/OCIDs):

```text
Session 1 (includes tofu apply)
12:57:36  tofu plan dump already in progress (init/plan start not in the copy)
12:57:42  tofu apply creating resources
12:58:24  Apply complete (21 added) → wait RUNNING / cloud-init
12:58:46  waiting for /etc/mcmgr/cloud-init-done … (SETUP-ISSUE-5 false wait)
13:17:59  last poll this session — do NOT use 19 min for cloud-init (use 3 min)

~40 min gap between sessions is idle, not deploy time

Session 2 (resume from tofu_applied)
13:57:07  Wrote tfvars; wait RUNNING
13:57:08  cloud-init ready (already done)
13:57:11  Door src / bootstrap start
14:02:05  Door bootstrap finished (4m54s)
14:02:05  Idle agent + VM1 Minecraft install
14:04:04  VM1 bootstrap finished (RCON list ok)
14:04:04  Repair door + VM1 runtime
14:04:29  VM1 runtime repaired
14:04:30  Seeding Object Storage
14:04:31  Published OS objects; Function skipped (no OCIR login)
```
